# Firebase / Firestore Configuration
# Set these as GitHub Secrets for GitHub Actions
# For local testing, set them as environment variables

FIREBASE_CREDENTIALS='{"type": "service_account", "project_id": "gainipo-prod", ...}'
FIRESTORE_PROJECT_ID=gainipo-prod

# Optional: Debug mode
DEBUG=false
